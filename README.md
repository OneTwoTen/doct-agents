# doct-agents

## Cách sử dụng:

1. Git submodule:

   ```bash
   git submodule add https://github.com/OneTwoTen/doct-agents.git 
   ```

2. Git subtree:

   ```bash
   git subtree add --prefix= https://github.com/OneTwoTen/doct-agents.git main --squash
   ```
